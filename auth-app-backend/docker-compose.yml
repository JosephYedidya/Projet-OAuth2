version: '3.8'

services:
  backend:
    build: .
    container_name: backend
    ports:
      - "5000:5000"
    env_file: .env
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge

